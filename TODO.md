# TODO - Pre-Release

- [ ] Disable `DUMP_JSON` flag in `TwitchOverlayMod.csproj`
- [ ] Add embedded lookup dictionary for enemies (sequential ID mapping like cards.json)
- [ ] Add embedded lookup dictionary for powers (sequential ID mapping like cards.json)
- [ ] Add embedded lookup dictionary for relics (sequential ID mapping like cards.json)
